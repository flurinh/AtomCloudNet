print("hello")

from Protos import *
"""
xyz, types, res = load_pdb(path=PATH, name='2L7B')
xyz = np.asarray(xyz)
res = np.asarray(res)
make_dist_file(xyz, types, res, name='2L7B', radius=6)"""

clean_XYZ()
clean_XYZ()